# API Specification

## REST Resources
GET /missions
GET /entities
GET /observations

POST /observations
POST /media
POST /verify

PATCH /entities/{id}

## Authentication
JWT via Supabase Auth

## Response
JSON only

## Version
/api/v1
